---------

Esta técnica se basa en usar **SQL Injection** para obtener la versión la versión de la base de datos. Lo único que cambia es que en este caso, lo haremos en ***MySQL*** y en ***Microsoft SQL***. 

## Explicación del laboratorio

Este laboratorio consiste es que tenemos una base de datos  y debemos obtener la versión que tiene la misma, en este caso ya sabemos que tecnología usa y podemos ir más al grano en lugar de probar de forma aleatoria.

![[31.png]]

----

### Resolución del laboratorio

Hay que tener en cuenta que para obtener la versión de la base de datos en **MySQL** y **Microsoft SQL**, se usa el mismo comando: ***SELECT @@version***. 

Al igual que en el caso anterior, tendremos que usar un ataque usando **UNION**: 

![[32.png]]


Como en el caso anterior, si accedemos al laboratorio, nos encontraremos con la misma página web: 


![[33.png]]


Debemos seleccionar una categoría dentro de todas las que hay disponibles, esto lo hacemos para activar el filtro y proseguir con la explotación, en mi caso, elegí la categoría *Accesories*: 


![[34.png]]

Por la forma en la que se organiza la información, podemos intuir que la web tiene 2 columnas: 

![[35.png]]

Con estos datos en mente, podemos sacar la versión usando la siguiente **SQL Injection**: 

![[36.png]]

Una vez ejecutada, ya tenemos la versión de la base de datos, como el laboratorio resuelto: 

![[37.png]]

![[38.png]]

-----

Básicamente, aplicamos una consulta con **UNION** para combinar la consulta original con el comando con el que obtendremos la versión de la base de datos. Como la web tiene 2 columnas, lanzamos un *NULL* o dato vacío para rellenar el primer espacio y en el segundo espacio metemos el comando que nos proporciona la versión.